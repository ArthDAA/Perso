#include <stdio.h>

int main()
{
    int a,b;            //L05

    a = 6;
    b = a + 2;
    if (a > b)          //L10
    {
        printf("%d est plus grand que %d\n", a, b);
    }
    return (0);
}